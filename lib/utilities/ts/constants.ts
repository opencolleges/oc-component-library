const NAMESPACE: string = `uds`;

const BASE_FONT_SIZE: string = `16px`;

const ELEMENT_SEPARATOR: string = `__`;
const MODIFIER_SEPARATOR: string = `--`;

export { BASE_FONT_SIZE, ELEMENT_SEPARATOR, MODIFIER_SEPARATOR, NAMESPACE };
